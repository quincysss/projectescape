# 06-05 前哨站状态机

> 来源文档：`06_前哨材料与前哨站修复系统_修订版_废土生存法则.md`
> 施工补充：开工前必须读取 `14_模块功能规则细化与AI开工提示词.md`。

## 1. 实现目标

为前哨站建立清晰状态机，统一候选、生成、待修复、修复中、已激活和失效等流程。

## 2. 状态建议

```gdscript
enum OutpostState {
    CANDIDATE,
    LOCKED,
    DAMAGED,
    REPAIRABLE,
    REPAIRING,
    ACTIVE,
    DISABLED
}
```

状态含义：

| 状态 | 说明 |
| --- | --- |
| CANDIDATE | 地图候选点，尚未被本局选中 |
| LOCKED | 已选中但还未暴露或初始化完成 |
| DAMAGED | 已出现，可查看需求但不可修复完成 |
| REPAIRABLE | 材料满足，可开始修复 |
| REPAIRING | 玩家正在读条修复 |
| ACTIVE | 修复完成，安全区/存储/撤离联动生效 |
| DISABLED | 局结束或特殊规则禁用 |

## 3. 转换规则

```text
CANDIDATE -> LOCKED -> DAMAGED
DAMAGED -> REPAIRABLE：需求满足
REPAIRABLE -> REPAIRING：玩家开始修复
REPAIRING -> REPAIRABLE：修复中断
REPAIRING -> ACTIVE：读条完成
ACTIVE -> DISABLED：局结束或清理
```

## 4. 状态职责

`OutpostEntity` 负责：

- 当前状态。
- 需求进度。
- 可交互提示。
- 修复读条。
- 激活后组件启用。

`OutpostManager` 负责：

- 创建前哨站。
- 保存前哨站列表。
- 广播激活状态。
- 与撤离/安全区/存储系统对接。

## 5. 信号

```gdscript
signal outpost_state_changed(outpost_id: String, old_state: int, new_state: int)
signal outpost_became_repairable(outpost_id: String)
signal outpost_activated(outpost_id: String)
signal outpost_disabled(outpost_id: String)
```

## 6. AI/MCP 开工提示词

```text
请实现前哨站状态机。

要求：
1. 状态包含 CANDIDATE、LOCKED、DAMAGED、REPAIRABLE、REPAIRING、ACTIVE、DISABLED。
2. 材料需求满足时从 DAMAGED 进入 REPAIRABLE。
3. 玩家开始修复时进入 REPAIRING，中断回到 REPAIRABLE。
4. 读条完成进入 ACTIVE，并发出 outpost_activated。
5. ACTIVE 后启用安全区、临时存储和撤离联动。
6. 所有状态转换写 Debug 日志。
```

## 7. 验收标准

- 状态转换路径可追踪。
- 不满足材料时不能进入修复读条。
- 中断后不会错误激活。
- ACTIVE 只触发一次。

## 8. 暂不实现

- 前哨站被敌人破坏。
- ACTIVE 后重新退化。
## BDD 场景补充

> 初始迁移：本节把本文规则挂接到 BDD 验收链路。后续重整本文时，应继续把关键成功、失败和边界规则拆成独立 Scenario。

```gherkin
Feature: 06-05 前哨站状态机
  作为 系统设计与实现线程
  我希望按本文规则完成 06-05 前哨站状态机
  以便对应功能、数据和验收保持一致

  Scenario: 按本文规则完成核心行为
    Given 已读取 `Doc/00_AI开工入口.md`
    And 已读取 `Doc/00_BDD需求文档规范.md`
    And 当前任务属于 "06_前哨材料与前哨站修复系统"
    When 根据本文新增、修改或验证对应功能
    Then 必须覆盖本文定义的前置条件、触发行为、结果反馈和边界情况
    And 必须把验证结果记录到本文验收标准或对应调试验证清单
```

验收方式：
- 文本验证：检查本文是否保留 `Feature`、`Scenario`、`Given`、`When`、`Then`。
- 执行验证：按本文既有“验收标准/调试工具/最小交付清单”执行。
- 缺口记录：若本文尚未拆出具体失败或边界场景，后续需求整理时继续补齐。
