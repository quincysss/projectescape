# 08-03 撤离交互与按住进度

> 来源文档：`08_撤离与结算系统_修订版_废土生存法则.md`
> 施工补充：开工前必须读取 `15_模块功能规则细化与AI开工提示词.md`。

## 1. 实现目标

实现撤离点按住交互，让撤离成为一个可被风险打断的最终动作。

## 2. 触发方式

玩家必须：

- 在撤离范围内。
- 满足撤离条件。
- 按住撤离键 E，或按住顶部撤离信号 UI。
- 没有其他读条或 UI 锁定。

## 3. 撤离进度

```gdscript
@export var extraction_hold_seconds: float = 3.0
```

流程：

```text
按下 E/撤离信号 UI -> 撤离信号 UI 内进度增长 -> 持续满足条件 -> 进度满 -> 撤离成功
```

## 4. 中断条件

- 离开家安全区。
- 松开按键。
- 受到关键打断。
- 稳定值归零。
- 对局时间归零。
- 撤离点状态失效。

V0.1 中断后进度重置。

## 5. AI/MCP 开工提示词

```text
请实现撤离按住交互。
要求：
1. 玩家在撤离范围内且条件满足时可按住撤离键。
2. 默认按住 3 秒完成撤离。
3. 离开范围、松开按键、稳定值归零、时间归零会中断。
4. 中断后进度重置。
5. 进度完成后调用 end_run(EXTRACTION_SUCCESS)。
6. 防止重复触发撤离成功。
7. 撤离进度显示在顶部撤离信号 UI 内，家中存储面板不再承载撤离按钮。
```

## 6. 验收标准

- 按住进度正常增长。
- 中断后不撤离。
- 完成后进入成功结算。

## 7. 暂不实现

- 队友等待撤离。
- 撤离载具动画。
## BDD 场景补充

> 初始迁移：本节把本文规则挂接到 BDD 验收链路。后续重整本文时，应继续把关键成功、失败和边界规则拆成独立 Scenario。

```gherkin
Feature: 08-03 撤离交互与按住进度
  作为 玩家
  我希望按本文规则完成 08-03 撤离交互与按住进度
  以便对应功能、数据和验收保持一致

  Scenario: 按本文规则完成核心行为
    Given 已读取 `Doc/00_AI开工入口.md`
    And 已读取 `Doc/00_BDD需求文档规范.md`
    And 当前任务属于 "08_撤离与结算系统"
    When 根据本文新增、修改或验证对应功能
    Then 必须覆盖本文定义的前置条件、触发行为、结果反馈和边界情况
    And 必须把验证结果记录到本文验收标准或对应调试验证清单
```

验收方式：
- 文本验证：检查本文是否保留 `Feature`、`Scenario`、`Given`、`When`、`Then`。
- 执行验证：按本文既有“验收标准/调试工具/最小交付清单”执行。
- 缺口记录：若本文尚未拆出具体失败或边界场景，后续需求整理时继续补齐。
