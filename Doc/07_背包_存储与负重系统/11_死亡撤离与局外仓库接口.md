# 07-11 死亡撤离与局外仓库接口

> 来源文档：`07_背包_存储与负重系统_修订版_废土生存法则.md`
> 施工补充：开工前必须读取 `16_模块功能规则细化与AI开工提示词.md`。

## 1. 实现目标

明确死亡失败、撤离成功、局外仓库之间的物品归属，供结算系统调用。

## 2. 死亡失败

死亡失败时：

- 局内背包按失败规则丢失或保留少量保险物。
- 前哨临时存储默认丢失。
- 家中存储不受影响。
- Debug 记录丢失列表。

V0.1 可采用简单规则：局内背包全部丢失，家中仓库保留。

## 3. 撤离成功

撤离成功时：

- 背包物品进入结算。
- 可选导入前哨临时存储。
- 结算后进入局外仓库或家中存储。
- 清空局内背包临时状态。

## 4. 局外仓库接口

```gdscript
func export_inventory_for_settlement() -> Array[Dictionary]
func import_items_to_home_storage(items: Array[Dictionary]) -> Dictionary
func clear_run_inventory()
```

## 5. AI/MCP 开工提示词

```text
请实现背包系统的死亡/撤离结算接口。

要求：
1. 死亡失败时导出丢失物品日志，局内背包清理，家中存储不受影响。
2. 撤离成功时导出背包物品给结算系统。
3. 结算系统可以把物品导入家中存储。
4. 前哨临时存储由 06 提供 payload，07 负责接收物品列表并尝试入库。
5. 所有导入必须处理空间不足，不得吞物。
```

## 6. 验收标准

- 死亡后局内物品按规则清理。
- 撤离后物品进入家中存储或结算列表。
- 家中仓库不会因死亡被清空。
- 空间不足有明确返回。

## 7. 暂不实现

- 保险箱保留机制。
- 死亡掉落尸体包。
## BDD 场景补充

> 初始迁移：本节把本文规则挂接到 BDD 验收链路。后续重整本文时，应继续把关键成功、失败和边界规则拆成独立 Scenario。

```gherkin
Feature: 07-11 死亡撤离与局外仓库接口
  作为 开发者
  我希望按本文规则完成 07-11 死亡撤离与局外仓库接口
  以便对应功能、数据和验收保持一致

  Scenario: 按本文规则完成核心行为
    Given 已读取 `Doc/00_AI开工入口.md`
    And 已读取 `Doc/00_BDD需求文档规范.md`
    And 当前任务属于 "07_背包_存储与负重系统"
    When 根据本文新增、修改或验证对应功能
    Then 必须覆盖本文定义的前置条件、触发行为、结果反馈和边界情况
    And 必须把验证结果记录到本文验收标准或对应调试验证清单
```

验收方式：
- 文本验证：检查本文是否保留 `Feature`、`Scenario`、`Given`、`When`、`Then`。
- 执行验证：按本文既有“验收标准/调试工具/最小交付清单”执行。
- 缺口记录：若本文尚未拆出具体失败或边界场景，后续需求整理时继续补齐。
