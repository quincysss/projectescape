# 07-14 调试工具与验证清单

> 来源文档：`07_背包_存储与负重系统_修订版_废土生存法则.md`
> 施工补充：开工前必须读取 `16_模块功能规则细化与AI开工提示词.md`。

## 1. Debug 面板字段

| 字段 | 用途 |
| --- | --- |
| inventory_slots_used | 背包占用格 |
| total_weight | 当前总负重 |
| weight_stage | 负重阶段 |
| selected_stack | 选中物品堆 |
| home_storage_used | 家中存储占用 |
| temporary_storage_count | 临时存储数量 |
| revision | 背包版本号 |

## 2. Debug 按钮

- Add Test Item
- Fill Inventory
- Clear Inventory
- Add Heavy Items
- Toggle Overweight
- Create Temp Storage
- Move Selected To Storage
- Export Settlement Payload
- Simulate Death
- Simulate Extraction

## 3. 验证清单

### 背包

- [ ] 物品可放入空格。
- [ ] 大尺寸物品占多格。
- [ ] 堆叠优先合并。
- [ ] 空间不足不吞物。

### 负重

- [ ] 背包重量计算正确。
- [ ] 装备重量计入。
- [ ] 超重仍可拾取。
- [ ] 移动惩罚接口可读。

### 存储

- [ ] 家中存储可双向转移。
- [ ] 前哨临时存储可创建和销毁。
- [ ] 空间不足不丢物。

### 结算

- [ ] 死亡清理局内背包。
- [ ] 撤离导出物品。
- [ ] 家中存储保留。

## 4. AI/MCP 开工提示词

```text
请实现 07 背包/存储/负重 Debug 面板和验证辅助。

要求：
1. 显示背包占格、总负重、负重阶段、选中堆叠、家中存储占用、临时存储数量、revision。
2. 提供添加测试物品、填满背包、清空背包、添加重物、创建临时存储、模拟死亡、模拟撤离等按钮。
3. 所有失败操作输出 reason。
4. 跑完背包、负重、存储、结算四类验证清单。
```

## 5. 验收标准

- Debug 能复现满包、超重、转移失败、死亡撤离。
- 验证清单覆盖 05 和 06 的依赖接口。
## BDD 场景补充

> 初始迁移：本节把本文规则挂接到 BDD 验收链路。后续重整本文时，应继续把关键成功、失败和边界规则拆成独立 Scenario。

```gherkin
Feature: 07-14 调试工具与验证清单
  作为 开发者
  我希望按本文规则完成 07-14 调试工具与验证清单
  以便对应功能、数据和验收保持一致

  Scenario: 按本文规则完成核心行为
    Given 已读取 `Doc/00_AI开工入口.md`
    And 已读取 `Doc/00_BDD需求文档规范.md`
    And 当前任务属于 "07_背包_存储与负重系统"
    When 根据本文新增、修改或验证对应功能
    Then 必须覆盖本文定义的前置条件、触发行为、结果反馈和边界情况
    And 必须把验证结果记录到本文验收标准或对应调试验证清单
```

验收方式：
- 文本验证：检查本文是否保留 `Feature`、`Scenario`、`Given`、`When`、`Then`。
- 执行验证：按本文既有“验收标准/调试工具/最小交付清单”执行。
- 缺口记录：若本文尚未拆出具体失败或边界场景，后续需求整理时继续补齐。
