# 07-15 V0.1 最小交付清单

> 来源文档：`07_背包_存储与负重系统_修订版_废土生存法则.md`
> 施工补充：开工前必须读取 `16_模块功能规则细化与AI开工提示词.md`。

## 0. 开工前统一检查

先读取：

```text
Doc/00_落地执行文档总则.md
Doc/07_背包_存储与负重系统/00_拆分规范与落地文档写法.md
Doc/07_背包_存储与负重系统/16_模块功能规则细化与AI开工提示词.md
```

开工前确认：

```text
[ ] 本模块作为 05 容器、06 前哨材料、08 结算的物品底座。
[ ] 拾取失败、空间不足、部分入包都不能吞物。
[ ] 超重只影响移动惩罚接口，不阻止拾取。
[ ] 丢弃 V0.1 直接销毁，不生成世界掉落。
```

完成后报告：

```text
物品定义：
背包数据：
存储数据：
负重接口：
外部调用接口：
不吞物验证：
未完成内容：
```

## 1. 最小闭环

```text
物品定义
-> 拾取入背包
-> 网格占格/堆叠
-> 负重刷新
-> 背包 UI 展示
-> 家中/前哨存储转移
-> 死亡/撤离结算
```

## 2. 必须交付

- ItemDefinition。
- ItemDefinition 售价字段：sellable、sell_currency_id、sell_value。
- ItemStack。
- InventoryGrid。
- try_add_item。
- 堆叠和占格。
- WeightComponent。
- 背包 UI。
- StorageContainer。
- 家中存储。
- 前哨临时存储接口。
- 转移接口。
- 丢弃销毁。
- 死亡/撤离导出接口。
- Debug 面板。

## 3. 可以延后

- 物品旋转。
- 高品质丢弃确认。
- 世界掉落。
- 复杂装备属性。
- 多人同步。
- 智能整理。

## 4. 推荐施工顺序

1. ItemDefinition 和 ItemStack，包含商人售卖所需字段。
2. InventoryGrid 占格。
3. try_add_item 和堆叠。
4. WeightComponent。
5. StorageContainer。
6. 转移接口。
7. 背包/存储 UI。
8. 死亡撤离接口。
9. Debug 验证。

## 5. AI/MCP 开工提示词

```text
请根据 07_背包_存储与负重系统拆分文档，在 Godot 中完成 V0.1 最小闭环。

优先实现：
1. ItemDefinition / ItemStack，包含 sellable、sell_currency_id、sell_value。
2. InventoryGrid 的占格、放置、移除、查找空位。
3. try_add_item：堆叠优先、支持部分成功、不吞物。
4. WeightComponent：负重阶段和移动惩罚接口。
5. StorageContainer：家中存储和前哨临时存储。
6. 背包/存储之间转移，保证不复制、不丢物。
7. 丢弃销毁，不生成世界掉落。
8. 死亡/撤离结算接口。
9. Debug 面板。

约束：
- 05 容器和 06 前哨必须通过本系统接口处理物品。
- 超重不阻止拾取。
- 空间不足不能吞物。
- 前哨存储不是永久仓库。
```

## 6. 完成定义

- 玩家可以拾取物品进背包。
- 背包满时物品不消失。
- 负重随物品变化。
- 超重仍能拾取并产生惩罚提示。
- 家中和前哨存储能转移物品。
- 死亡/撤离能导出正确物品归属。
## BDD 场景补充

> 初始迁移：本节把本文规则挂接到 BDD 验收链路。后续重整本文时，应继续把关键成功、失败和边界规则拆成独立 Scenario。

```gherkin
Feature: 07-15 V0.1 最小交付清单
  作为 系统设计与实现线程
  我希望按本文规则完成 07-15 V0.1 最小交付清单
  以便对应功能、数据和验收保持一致

  Scenario: 按本文规则完成核心行为
    Given 已读取 `Doc/00_AI开工入口.md`
    And 已读取 `Doc/00_BDD需求文档规范.md`
    And 当前任务属于 "07_背包_存储与负重系统"
    When 根据本文新增、修改或验证对应功能
    Then 必须覆盖本文定义的前置条件、触发行为、结果反馈和边界情况
    And 必须把验证结果记录到本文验收标准或对应调试验证清单
```

验收方式：
- 文本验证：检查本文是否保留 `Feature`、`Scenario`、`Given`、`When`、`Then`。
- 执行验证：按本文既有“验收标准/调试工具/最小交付清单”执行。
- 缺口记录：若本文尚未拆出具体失败或边界场景，后续需求整理时继续补齐。
